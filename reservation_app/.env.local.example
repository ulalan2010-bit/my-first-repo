# Supabaseダッシュボード(Project Settings > API)から取得して .env.local にコピーする。
# .env.local はコミットしない(.gitignoreで除外済み)。

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# サーバー専用(Route Handler / Server Action限定で使用、クライアントに絶対公開しない)
SUPABASE_SERVICE_ROLE_KEY=
